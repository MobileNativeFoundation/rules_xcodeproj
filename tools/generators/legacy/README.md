# Legacy `.xcodeproj` generator

This is the original `.xcodeproj` generator, which is being being replaced by
the [“incremental generator”](../README.md). It creates the entire `.xcodeproj`
file bundle at one time.
