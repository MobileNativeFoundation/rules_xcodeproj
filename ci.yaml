version: 1
on:
  pull_request:
    - jobs:
        - script: snapci/pr.sh
          run_on: "mac_studio"
  cool:
    job:
      script: snapci/cool.sh
      run_on: "mac_studio"
  comment:
    - body: "/deploy"
      jobs:
        - script: snapci/cool.sh
          run_on: "mac_studio"
